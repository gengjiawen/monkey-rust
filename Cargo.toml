[workspace]
members = [
  "lexer",
  "parser",
  "gc",
  "object",
  "interpreter",
  "compiler",
  "asm",
  "wasm"
]

[workspace.lints.clippy]
# Explicit `return` is the preferred style in this codebase.
needless_return = "allow"
